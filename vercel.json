{
    "version": 2,
    "builds": [
      {
        "src": "next.config.js",
        "use": "@vercel/next"
      }
    ],
    "buildCommand": "prisma generate && next build",
    "env": {
      "DATABASE_URL": "mysql://root@localhost:3306/reach_db"
    }
  }
  